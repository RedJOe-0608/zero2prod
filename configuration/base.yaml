# Settings identical in EVERY environment.
# (differs-per-env values live in local.yaml / production.yaml;
#  secrets like the db password live in env vars, never here.)
application:
  port: 8000
database:
  port: 5432
  database_name: "newsletter"
